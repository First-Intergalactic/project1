[build]
  base = "client"
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Update this URL after deploying backend to Render
  VITE_API_URL = "https://dating-app-api.onrender.com/api"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
